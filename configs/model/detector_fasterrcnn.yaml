_target_: disease_detection.models.detector.FasterRCNNModule
# background + plant_roi (AIhub 스키마에 plant part 라벨 없음 → single-class).
num_classes: 2
lr: 0.005
momentum: 0.9
weight_decay: 5e-4
lr_step: 10
lr_gamma: 0.1
