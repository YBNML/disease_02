# @package _global_
# 화상병 이진 분류기 학습. 라벨은 AIhub `disease_code != 0` 이진화.
defaults:
  - override /data: aihub_combined
  - override /model: classifier_resnet18
  - override /trainer: ubuntu_gpu

run:
  name: fireblight_baseline

classifier:
  kind: fireblight
  label_source: aihub
  pos_weight: null
