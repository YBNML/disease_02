accelerator: gpu
devices: 1
precision: "16-mixed"
max_epochs: 50
gradient_clip_val: 1.0
log_every_n_steps: 20
early_stopping:
  monitor: val/loss
  patience: 5
  mode: min
checkpoint:
  monitor: val/loss
  mode: min
  save_top_k: 3
  filename: "{epoch}-{val/loss:.4f}"
